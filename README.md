# e-commerce

1. Clonar el respoistorio
2. Clonar el archivo .env.temaplte y renombrarlo a .env
3. Ejecutar el comando `npm install` para instalar dependencias
4. Verificar que el backend este corriendo en el puerto correcto
5. Ejecutrar el comando `npm run dev` para iniciar el servidor de desarrollo